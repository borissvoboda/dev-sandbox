let arr = [1, 'two', 'three', 'four', 'five', 'six', 'seven', 'eight', false];

// // push adds to the end of the array
// arr.push('konec');

// // unshift adds as the first element
// arr.unshift(0);

// // shift removes first element and return it
// firstEl = arr.shift();

// // modify index
// arr[0] = 'one';

// // join all el's with a symbol
// arr.join(';');

// // Slice - subarray of els from index 1 (incl) to 4 (excl)
// arr = arr.slice(1, 4);

// // Splice - remove 4 elements starting index 2, insert subsequent elements
// // function returnes removed subarr!!
// arr1 = arr.splice(2, 4, false, true, false, true);

console.log(arr);
