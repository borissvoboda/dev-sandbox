const text = `
In Rust there has to be specified size.

In general:
Array, you can re-allocate it; create a new array.

In JS, or JavaScript you do not see underlying array.

------------

In Rust, when you create a new vector, its gonna create a memory buffer with the size of 5, to play with.
Without need to reallocate. 
The question is, what wiggling buffer is reasonable.

`;
